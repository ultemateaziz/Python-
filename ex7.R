for (val in 1: 5)
{
  # statement
  print(val)
}
week <-c('Sunday',
         'Monday',
         'Tuesday',
         'Wednesday',
         'Thursday',
         'Friday',
         'Saturday')

# using for loop to iterate
# over each string in the vector
for (day in week)
{
  
  # displaying each string in the vector
  print(day)
}

val = 1

# using while loop
while (val <= 5)
{
  # statements
  print(val)
  val = val + 1
}
